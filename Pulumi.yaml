name: neo-infra-1
description: AWS EKS cluster with t3.small node pool
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
